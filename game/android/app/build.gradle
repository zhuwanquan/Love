apply plugin: 'com.android.application'

android {
    namespace "com.rc7.oneyearroom"
    compileSdk rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "com.rc7.oneyearroom"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        aaptOptions {
            // 排除版本控制与系统文件，保留所有 web 资源
            ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

// === Web 资源同步 ===
// 将 game/www/ 复制到 assets/public/，确保构建时使用最新游戏文件
task copyWebAssets(type: Copy) {
    from '../../www'
    into 'src/main/assets/public'
    include '**/*'
}

preBuild.dependsOn copyWebAssets

// 排除旧版 Kotlin stdlib（与 AGP 8.x 内置 kotlin-stdlib 1.8.22 冲突）
// kotlin-stdlib 1.8+ 已内置 jdk7/jdk8 扩展，无需单独依赖
configurations.all {
    exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
    exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
}

dependencies {
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core:$androidxCoreVersion"
    implementation "androidx.webkit:webkit:$androidxWebkitVersion"

    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
}
